<%= form_for(@mod) do |f| %>
  <% if @mod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mod.errors.count, "error") %> prohibited this mod from being saved:</h2>

      <ul>
      <% @mod.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Tipo %><br>
    <%= select("mod", "shape_id", Shape.all.collect {|s| [ s.description, s.id ] })
     %>
  </div>
  <div class="field">
    <%= f.label :descrizione %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit "Salva", :class=>"button large"%>
  </div>
<% end %>
