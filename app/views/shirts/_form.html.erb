<%= form_for(@shirt) do |f| %>
  <% if @shirt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shirt.errors.count, "error") %> prohibited this shirt from being saved:</h2>

      <ul>
      <% @shirt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descrizione %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br>
    
    <%= f.collection_select :shape_id, Shape.all, :id, :description %>
  </div>
 
  <div class="actions">
    <%= f.submit "Salva", :class=> "button large" %>
  </div>
<% end %>
